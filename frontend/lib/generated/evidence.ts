// GENERATED by scripts/17_export_frontend_data.py -- do not edit.
//
// Kept out of `index.ts` on purpose: that barrel is a single module imported by
// the root layout, so anything in it is bundled into all fifteen routes. This
// payload is large, so it lives here and is bundled only where it is imported.
//
// Assigned to its declared type WITHOUT a cast, exactly as the barrel does.

import payload from './evidence.json';

import type { GeneratedEvidence } from './types';

/** Every exported column, figure and page payload, mapped to the CSV it was read from and that file's digest. The evidence browser's data (T117.4). */
export const evidence: GeneratedEvidence = payload;
