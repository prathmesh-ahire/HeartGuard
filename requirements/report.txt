# PV-MEPCG / PulseVision — document and workbook generation (Part IX)

python-docx==1.2.0   # .docx thesis and audit report generation
openpyxl==3.1.5      # .xlsx table workbooks

# Product Reports page (Phase 134, T134.1/T134.5): per-recording and
# model-summary PDFs. python-docx is not reused here -- the operator-facing
# Reports page wants a file every viewer can open with no application
# installed, which DOCX does not guarantee and PDF does. reportlab is
# pure-Python (no compiler toolchain, verified on Windows + 3.11.9). pypdf
# reads a rendered PDF back for T134.7's round-trip test, the same role
# python-docx plays for the DOCX reports.
reportlab==5.0.1     # PDF report generation
pypdf==6.19.0         # read a generated PDF back, for the T134.7 test

# kaleido: static image export for Plotly figures.
# WINDOWS NOTE (Phase 02): kaleido 1.x dropped the bundled Chromium that 0.2.1
# shipped and now drives an EXTERNAL Chrome/Chromium via choreographer. No
# Chrome is installed on this machine, so kaleido cannot render until either
# `kaleido_get_chrome` is run (downloads Chromium) or a system Chrome exists.
# It is imported by scripts/verify_env.py but NOT exercised — nothing in the
# pipeline currently renders Plotly from Python (all figures are matplotlib /
# seaborn; Plotly is a frontend-JS concern). See Docs/note.md.
kaleido==1.3.0

# Diagram toolchain (T97.1): matplotlib, already pinned in base.txt.
# NOT Graphviz -- needs a `dot` system binary that is not on PATH here and will
# not be on a grader's either. NOT the Mermaid CLI -- it pulls a headless
# Chromium, the same dependency that already leaves kaleido above unusable on
# this machine. matplotlib renders SVG and 300 dpi PNG natively, runs offline,
# and keeps every diagram a version-controlled Python function under
# src/reporting/diagrams/. Nothing further to install.
