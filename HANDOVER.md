<!-- GENERATED by scripts/51_package_delivery.py -- do not edit by hand. -->

# PV-MEPCG / PulseVision — handover

Generated 2026-09-12 from the repository itself.

> **Scope.** An academic screening and decision-support prototype. Not a medical
> device, not a diagnostic tool, and not clinically validated in any way. Every
> result below is a cross-validation within public research corpora.

## What was produced

| Deliverable | Target | Produced |
|---|---|---|
| tables | 30 | 30 |
| graphs | 35 | 35 |
| diagrams | 20 | 20 |
| algorithms | 20 | 20 |
| screenshots | 13 | 13 |

The extraction spec and the blueprint between them name **340 required
items**. 335 are present, 5 are recorded as not
produced with a technical reason, and 0 are failed (missing with no
reason). The item-by-item list is `outputs/00_evidence_index/evidence_index.xlsx`.

Also produced, and worth opening first:

| File | What it tells you |
|---|---|
| `outputs/00_evidence_index/evidence_index.xlsx` | every artifact, the command that made it, its sources, and whether it is on disk |
| `outputs/00_evidence_index/final_qa_report.md` | the six-area QA sweep, recomputed from the committed files |
| `outputs/00_evidence_index/compliance_review.md` | the claims review: language, disclaimer, perfection, objectives, counts, provenance |
| `outputs/00_evidence_index/run_manifest.json` | every run, its seed, its package versions and its wall time |
| `outputs/missing_outputs_report.txt` | **everything that could not be produced, and why** |

## What was not done, and why

**Blocked tasks (`[!]`):**

- **T121.1** [TEST] Clean build from an empty `outputs/`: full pipeline, exporter, frontend build, guard rail — one command, no manual intervention.
- **T122.3** Verify a clean end-to-end run from an empty `outputs/` and record the total wall time.
- **T122.7** [TEST] Run `scripts/00_run_everything.py` from an empty `outputs/`; confirm it completes, and that a second run reproduces identical metrics.

**Skipped tasks (`[-]`):**

- **T64.5** Evaluate all models on the untouched 301-record PhysioNet validation set.

The gaps report, verbatim:

```
================================================================================
PV-MEPCG / PulseVision — MISSING OUTPUTS REPORT
================================================================================

Purpose
-------
Every deliverable named in the source documents that this project could NOT
produce is recorded here, with the technical reason. Ground rule 1: a number is
never invented and a gap is never silently omitted. Ground rule 7: this file is
created empty in Phase 01 and APPENDED TO as gaps arise — it is never written at
the end from memory, because by then the reasons are forgotten.

A `[-]` (skipped) checkbox in `Docs/todo.md` REQUIRES a matching entry here and
a matching entry in `Docs/note.md`.

Entry format
------------
[YYYY-MM-DD] <task id> — <deliverable that is missing>
  Reason: <the technical reason it could not be produced>
  Impact: <what claim or figure is affected downstream>

--------------------------------------------------------------------------------
ENTRIES (newest last)
--------------------------------------------------------------------------------
[2026-08-26] T20.5 — PhysioNet 2016 held-out external test set (301 validation records)
  Reason: All 301 records in `dataset/archive (3)/validation/` are BYTE-IDENTICAL
    (SHA-256 of raw bytes) to 301 records already present in training-a..f. They
    contain no audio the training corpus does not already contain. Those 301
    twins belong to 205 derived subjects, and those 205 subjects account for
    1,108 of the 3,240 training records — so constructing a subject-disjoint
    holdout from this folder (rule 3) would remove 34% of the primary track's
    training data in exchange for a test set of previously-seen material.
    Verified in Phase 09 (physionet_subject_derivation.csv) and Phase 17
    (duplicate_report.csv: 301 rows, method=raw_sha256, decision=drop).
  Impact: EXP-A1 and EXP-A2 report repeated 5x5 subject-grouped cross-validation
    only; `external_test` is null for both in configs/experiments.yaml. No
    single-number "held-out test" figure is claimed for PhysioNet anywhere in
    the thesis, paper or dashboard. External validation for the binary track is
    carried by EXP-D1 (PhysioNet -> CirCor), which uses a genuinely independent
    corpus. Decision confirmed with the user 2026-08-26.


[2026-08-26] T29.3 / T29.4 / T29.7 — PP-09 `preprocessing_ablation.csv` (deferred, not skipped)
  *** RESOLVED 2026-09-10. PP-09 exists. The entry below is kept for the record;
      the gap it describes is closed. The run took 2 h 06 m over the full 3,240
      supervised D1 records under all four filter x normalization arms, using the
      locked 138 features, the DA-07 25-fold map and an untuned M1 baseline.
      Two controls passed: the re-extracted shipped arm PP-A matches the
      committed FE-03 matrix to a maximum relative difference of exactly 0, and
      PP-A's cross-validated score reproduces EXP-A1's M1 row to six decimals.
      Finding: all four arms are indistinguishable (every delta an order of
      magnitude below the fold SD), and the shipped arm is NOT the best on
      balanced accuracy or AUC. The standing rule that no preprocessing claim
      may cite a measured metric delta therefore REMAINS in force, for the
      opposite reason to the original one — the deltas exist now, and they do
      not support such a claim. See Docs/note.md, 2026-09-10. ***
  Reason: The task asks for features to be extracted and a baseline model trained
    under each of the four filter x normalization configurations. Neither input
    exists at Phase 29: the locked 138-feature extractor is built in Phases 31-42
    and the fold-safe CV pipeline, metrics module and baseline models in Phases
    43-46. The only way to produce PP-09 on schedule would be to write a
    throwaway feature set and model for this one table, which would place metrics
    in a deliverable that the actual pipeline never produced — the failure mode
    ground rule 1 exists to prevent.
  Impact: T29.7, the Phase 29 gate, is blocked on the same gap: its PP-07 and
    evidence-registration clauses pass, its "the ablation ran all four
    configurations" clause cannot until PP-09 exists.
    PP-09 is registered in the evidence index with status=missing rather
    than omitted, so the gap is visible. The four arms ARE defined and verified
    (T29.2 complete): `preprocessing_ablation_grid.csv` holds their config hashes
    and cache directories, and all four were confirmed to produce distinct
    preprocessed signals, so the ablation is ready to run rather than merely
    planned. Re-entry point: immediately after Phase 46, using the real 138
    features, the Phase 44 fold-safe pipeline and a baseline model, so PP-09 is
    comparable with every other metric in the thesis. Decision taken with the
    user 2026-08-26. No preprocessing claim anywhere may cite an ablation result
    until that run exists — the justification for the shipped configuration is,
    until then, the design argument in Docs/note.md and the T24/T26 measurements,
    not a measured metric delta.

[2026-08-27] T52.2 / T52.3 / T52.4 / T52.5 — M9, the optional 1D-CNN deep baseline
  Reason: Decided OUT OF SCOPE by the user on 2026-08-27, against the measured
    cost of the classical stack rather than against an assumption. This machine
    is CPU only — 4 logical cores, no CUDA device — and both source documents
    list the 1D-CNN as Optional.
    The measurement that settled it: all eight classical models together fit one
    fold of the 3,240-record PhysioNet track in 41.1 s wall clock (M1 0.23 s,
    M2 0.05 s, M3 1.34 s, M4 5.84 s, M5 27.37 s, M8 6.31 s; M6/M7 add ~163 s
    each because they refit their members over an inner CV). The full 5x5
    grouped fold map is therefore ~17 minutes for the classical stack.
    A 1D-CNN over fixed-length preprocessed segments is not comparable to that
    on this hardware: it trains on raw waveform segments rather than on 138
    precomputed scalars, so a single fold is hours and the full fold map is days,
    with no GPU to shorten it. Running it on a reduced fold map instead would
    break the comparison the model exists to support — T52.4 requires it to be
    trained under the SAME fold map as the classical models, or its accuracy
    cannot be placed beside theirs.
    It would also add an unpinned deep-learning dependency (PyTorch or Keras,
    neither currently in requirements/) that would have to be re-verified to
    install and run on Windows + Python 3.11.9 with no compiler toolchain.
  Impact: No deep-learning baseline appears in the model comparison. The
    complexity trade-off argument (T25/T26, Phase 75) rests on the eight
    classical models only, and the thesis limitations section must state that a
    deep baseline was excluded for hardware reasons rather than tested and
    rejected on performance. No claim anywhere may compare PV-MEPCG against a
    CNN, or describe the classical stack as outperforming one.
    M9 remains declared in configs/models.yaml with `in_scope: false` so the
    exclusion is visible in the model registry (`registry_frame()` reports it as
    not implemented with this reason) rather than looking like an oversight.


[2026-08-28] T64.5 — EXP-A1 evaluation on the 301-record PhysioNet validation set
  Reason: The set does not exist as a held-out test set. See the T20.5 entry
    above: all 301 records in `dataset/archive (3)/validation/` are byte-identical
    duplicates of records already in training-a..f, belonging to 205 subjects
    that carry 1,108 of the 3,240 training records. Scoring EXP-A1's models on
    them would report performance on rows those models were trained on, for 25
    of 25 folds — a number that would look like external validation and would be
    training accuracy. `external_test` is null for EXP-A1 and EXP-A2 in
    configs/experiments.yaml, and the runner has no path that would score one.
…
```

## The five things a reader should not misread

1. **The binary model does not transfer across recording collections.** AUC falls
   from 0.92-0.94 pooled to 0.43-0.53 leave-one-sub-collection-out. The pooled
   numbers are correct as a within-corpus cross-validation and are not a
   generalization claim.
2. **The final binary model (M1, a regularised logistic regression) is not better
   than the ensemble.** They are statistically tied (sensitivity 0.8648 vs 0.8615,
   p = 0.797); M1 is simpler and 190x faster. Never write that it outperformed.
3. **Accuracy would have chosen a different model, and that is the point.** The
   selection rule is sensitivity then balanced accuracy; every deployed model
   records what raw accuracy would have picked instead.
4. **`artifact` in PASCAL A is a recording-quality label**, not a cardiac class.
5. **EXP-D1's drop is a population and acquisition effect**, not a failure of the
   method.

## What clinical validation would require

Nothing in this repository is a step toward clinical use, and the gap is larger
than "more data". At minimum, the following would have to happen first, roughly
in this order:

1. **A prospective, multi-site acquisition protocol.** The single largest
   measured weakness here is that performance does not survive a change of
   recording collection: leave-one-sub-collection-out takes AUC from 0.92-0.94
   to 0.43-0.53, several folds below chance. Any clinical study must acquire on
   the devices, in the rooms, and with the operators it intends to deploy to,
   and must hold out entire sites.
2. **A reference standard that is not another label file.** These corpora are
   labelled by expert listening and, for CirCor, by a published protocol. A
   clinical study needs echocardiography (or an equivalent) as ground truth,
   read blind to the model output.
3. **A pre-registered operating point and endpoint.** The decision threshold
   here is chosen inside each training fold against balanced accuracy. A
   clinical study fixes one threshold in advance, states the sensitivity and
   specificity it is powered to detect, and reports what it pre-registered.
4. **Paediatric and adult cohorts studied separately.** EXP-D1 is
   adult-to-paediatric transfer and drops as a population effect; the two are
   not one population and cannot share a model, a threshold, or a claim.
5. **A clinically meaningful comparator.** "Better than chance" is not a claim
   anyone can act on. The comparator is a clinician with a stethoscope, and the
   question is whether a screening aid changes referral decisions.
6. **Human-factors and failure-mode work.** What a clinician does with a
   low-confidence output, what happens on an unscorable recording, and what the
   cost of a false negative is in the intended care pathway.
7. **Regulatory scope.** Software intended to inform a clinical decision is a
   medical device in most jurisdictions, and PV-MEPCG is not a medical device,
   is not built to any quality management standard, and would need to be
   rebuilt under one.

Until every one of those exists, the correct description of this work is the one
on the first page of the README: an academic screening and decision-support
prototype, evaluated within public research corpora.
